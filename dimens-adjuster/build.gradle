apply plugin: 'groovy'
apply plugin: 'maven'




dependencies {
    implementation fileTree(dir: 'libs', include: ['*.jar'])
    // gradle sdk
    implementation gradleApi()
    // groovy sdk
    implementation localGroovy()
}
group='com.helloyuyu.dimenadjuster'
version='1.0.5'

uploadArchives {
    repositories {
        mavenDeployer {
            repository(url: uri('../repo'))
        }
    }
}